import { ReactNode } from 'react';

type PriceCardProps = {
  text: string;
  price: number;
};
const PriceCard = ({ text, price }: PriceCardProps) => {
  return <div></div>;
};

export default PriceCard;
